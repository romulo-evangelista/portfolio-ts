import { Menu } from './components/Menu';

import './index.css';

export const Header = () => (
  <div className="header-container">
    <div className="header-logo">LOGO</div>
    <Menu />
  </div>
);